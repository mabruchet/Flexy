<div class="Story">

    <div class="Story-section">
        <h4 class="Story-title">Base (LiveComponent — the whole tunnel of the session cart, one section per configured step)</h4>
        <div class="Story-col">
            <span class="Story-label">renders the steps `checkout_display_mode = one_page` shows on /checkout/cart: each active step of the configuration is an accordion section, opened while the session cart has earned it and locked past the first incomplete one — a locked section keeps its content out of the DOM. With no cart in the session, only the cart section shows.</span>
            <twig:Organisms:CheckoutOnePage:Base />
        </div>
    </div>

</div>
